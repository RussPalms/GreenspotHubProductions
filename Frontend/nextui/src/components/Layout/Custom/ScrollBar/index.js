import { ScrollBarContainer } from "./ScrollBarElements";

const ScrollBar = () => {
    return (
        <>
            <ScrollBarContainer>
                
            </ScrollBarContainer>
        </>
    )
}

export default ScrollBar
